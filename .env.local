# JWT secret — change this to a random 32+ character string in production
JWT_SECRET=dev-secret-change-me-in-production-please

# WebAuthn configuration
RP_NAME=Todo App
RP_ID=localhost
RP_ORIGIN=http://localhost:3000
